import React from 'react';

import Layout from '../components/Layout';
import Player from '../components/Player';

const Home = ()=>{
  return(
    <Layout>
      <Player />
    </Layout>
  );
};

export default Home;